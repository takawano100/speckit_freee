# セットアップ記録（2026-08-16）

記事（軸B・要求AI連載）用の実測メモ。**098〜108でやった Spec Kit 検証からの差分**を残す。

## 環境

| | |
|---|---|
| Spec Kit | `uvx --from git+https://github.com/github/spec-kit.git specify init` |
| コミット | `bf88c9f9a82fa370c7a7257aa2b3cf10b457b65c` |
| 導入コマンド | `specify init --here --force --script ps --integration claude --ignore-agent-tools` |
| uv | 0.10.0 |
| freee MCP | ユーザースコープ（`~/.claude.json` の top-level `mcpServers`）。この直下でもそのまま使える |
| freee 事業所 | 開発用テスト事業所 `12755401`（**3ヶ月削除に引っかかっていなかった**） |

## ★098の時から変わっていたところ（定点観測のネタ）

1. **`--ai` オプションが無くなり、`--integration` になった。**
   `specify init --ai claude` はエラー（`No such option: --ai`）。
2. **コマンドが「スキル」として入るようになった。**
   098では「スキル10本＋テンプレート5本＋PowerShellスクリプト5本」と書いたが、
   いまは `.claude/skills/speckit-*/SKILL.md` の形で入る（10個）。
3. **コマンドが2つ増えていた。**
   - `/speckit-converge` … コードベースを評価して、残作業をタスクとして追記する
   - `/speckit-taskstoissues` … タスクをIssueにする
4. **`.specify/workflows/` が増えた。** `workflow.yml` と `workflow-registry.json`。
   工程が YAML で定義される形になっている（098の時は無かった）
5. **`--preset` と `--extension` が増えた。** 拡張を入れる仕組みができている。

→ **「Spec Kit は工程そのものを設定ファイルにし始めた」**という見立てが立つ。
   ただし中身は未確認。`workflow.yml` を読んでから書く。

## 現在の構成

```
C:\Users\takawano\speckit_freee\
  .claude\skills\speckit-{analyze,checklist,clarify,constitution,converge,
                          implement,plan,specify,tasks,taskstoissues}\SKILL.md
  .specify\
    memory\constitution.md
    templates\{spec,plan,tasks,checklist,constitution}-template.md
    scripts\powershell\*.ps1
    workflows\speckit\workflow.yml
  REQUEST.md        ← /speckit-specify に渡す入力（4視点で書いた要求）
  SETUP_LOG.md      ← この文書
```

## 2026-08-16 17:10 疎通結果

新しい開発用テスト事業所 **`12788365`**（法人／会計プロフェッショナル／人事労務エンタープライズ）。

| API | 結果 |
|---|---|
| 会計 `GET /api/1/sections`（部門） | ✅ **通った**。営業部・営業部門・開発部門の3件が既に入っている |
| 会計 `GET /api/1/account_items`（勘定科目） | ✅ 通った（5,389行） |
| 人事労務 `GET /api/v1/users/me` | ✅ 通った。role=company_admin。**ただし employee_id は null** |
| 人事労務 `GET /api/v1/employees` | ⚠️ 400 **「事業所設定を完了させて下さい」** ← UIで初期設定が要る |
| **工数管理 `GET /projects`** | ❌ **401「存在しないかすでに削除された事業所です」** |

### ★工数管理は使えない（確定）

**スコープ（`pm:projects:read` 等）は付与されているのに401。**
アプリの権限にはあるが、**この事業所に freee工数管理という製品が入っていない。**
テスト事業所の作成画面で選べたのは **freee会計と freee人事労務だけ**だった。

→ **当初予定していた「工数→会計をつなぐ」は、この環境では成立しない。**
→ **題材を「勤怠に"何の仕事か"を足して、労務費を会計に載せる」に変更する。**
   これは技術部時代とまったく同じ形（勤怠はある／工数が無い／だから打刻に指図書番号と工程番号を足した）。

### ★403の本当の原因（記事にできる）

1. `freee_api_list_paths` が返す約330本は **MCPサーバ同梱のOpenAPI定義**であって、
   **アクセス権があることの証明ではなかった**
2. アプリの権限設定でチェックを入れても、**右上の「下書き保存」を押さないと保存されない**
   （ブラウザ幅が狭いとボタンが画面外に出る）
3. 権限を保存しても、**発行済みトークンには古いスコープが焼き付いたまま**。再認証が必須
4. 再認証しても、**MCPサーバがメモリ上の古いトークンを持っている間は403のまま**

→ **「権限がある」は4段階ある**：定義にある／アプリに許可されている／トークンに入っている／
   プロセスが読み直している。**どれか1つ欠けても同じ403が出る。**

## 2026-08-16 19:5x 人事労務の初期設定をブラウザ操作で実施

3ステップとも完了させた（画面に「3ステップ設定が完了しました！」）。入力値は全部**架空**。

| ステップ | 入れた値 |
|---|---|
| ①給与規定 | 月末締め・**翌月25日払い**／社保徴収=翌月／協会けんぽ・東京都／雇用保険=一般事業 |
| ②働き方 | 名称「正社員」／**勤怠管理の方法＝freee人事労務で勤怠登録する**（★打刻APIの前提）／**固定時間制**／9:00-18:00・休憩12-13・1日8時間／法定休日=日曜／平均所定労働日数21日／割増率は法定どおり |
| ③従業員 | **テスト太郎**（架空）／入社2026-04-01／生年月日1990-01-01／勤務・賃金設定=正社員／**月給 300,000円**／招待・入社手続き依頼は**なし**（メール送信なし） |

### ★給与計算は「やらない」ではなく「要る」（2026-08-16 訂正）

当初「給与計算は検証しない」と書いたが誤り。**労務費＝時間×単価で、単価は給与から出る。**
- 単価の分母＝②の**1ヶ月の平均所定労働時間**
- 残業が労務費に効く＝②の**時間外125%・深夜25%**
- → **`REQUEST.md` の「単価はこちらで持つ」も誤り。人事労務の給与から出すのが正しい**

### API の状態（設定直後）

| | 結果 |
|---|---|
| `GET /api/v1/companies/12788365/employees` | ✅ **エラーが消えた**（設定完了の効果）。ただし **`[]`（0件）** |
| `GET /api/v1/employees?year=&month=` | ❌ まだ「事業所設定を完了させて下さい」 |

### ★★事業所を取り違えていた（20:00 判明・解決）

**ブラウザの freee人事労務は、新しい `12788365` ではなく古い `12755401` にログインしていた。**
（切り替えメニューに1件しか出なかったので新しいほうだと判断したが、逆だった。
　**人事労務が有効なのは古い `12755401` のほう**で、新しい `12788365` は会計だけ）

**結果的に問題なし。`12755401` に両方揃っている:**

| | `12755401`（7/22作成・**これを使う**） | `12788365`（8/16作成） |
|---|---|---|
| 会計 部門 | ✅ 営業部・営業部門・開発部門 | ✅ 同じ3件 |
| 人事労務 | ✅ **設定完了・従業員あり** | ⚠️ 空（未設定） |

**→ 以後 `12755401` を使う。** `freee_set_current_company` で設定済み。

### ★疎通確認（20:0x）— 題材が成立することを確認

**従業員**: `id 4174944` / テスト 太郎 / entry_date 2026-04-01 / payroll_calculation true /
closing_day 31 / pay_day 25 / month_of_pay_day next_month

**`GET /api/v1/employees/4174944/work_records/2026-08-14`** ✅ 取得成功
```
normal_work_clock_in_at  : 2026-08-14T09:00:00+09:00   ← ②で設定した所定
normal_work_clock_out_at : 2026-08-14T18:00:00+09:00
normal_work_mins         : 480                          ← 1日8時間
total_overtime_work_mins / total_latenight_work_mins /
total_holiday_work_mins / total_excess_statutory_work_mins ← 残業・深夜・休日の内訳を持っている
break_records / work_record_segments                     ← 明細の器がある
```

**`GET /api/v1/employees/4174944/time_clocks/available_types`** ✅ → `["clock_in"]`
（打刻の状態遷移をAPIが持っている。いまは未出勤なので次に打てるのは出勤だけ）

**→ 勤怠は完全に使える。工数（何の仕事か）だけが無い。題材が成立した。**

**★ここが記事の芯になる**: `work_record_segments` という**明細の器が既にある**のに、
**「何の仕事に対する時間か」を入れる場所が無い。** 40年前、打刻に指図書番号と工程番号を足したのと
同じ穴が、そのまま空いている。

### ブラウザ操作でハマった点（記事ネタになる）

**招待「招待する」と入社手続き依頼「依頼する」が既定でオン**で、メールアドレスと期限が必須になり、
追加ボタンが押せなかった。`form_input` でチェックを外すと**見た目は外れるがフォームの状態は変わらない**
（Reactのstateが更新されない）。**実クリック2回**で同期させて解決した。
→ **「画面では外れているのに、システムは外れていないと思っている」**＝
   [[insight-ui-basedesign-in-spec]]（白地に白文字がすり抜けた話）と同型。

## ★再開ポイント（2026-08-16 17:15 中断）

**認証・権限まわりは全部片付いた。残りは freee人事労務の初期設定だけ。**

### いまの状態

- 事業所 **`12788365`**（法人／会計プロフェッショナル／人事労務エンタープライズ・アドバンスプラン）
- トークンは全スコープ入り（`hr:time_clocks:*` `hr:work_records:*` `accounting:sections:*` など）
- **会計は完全に使える**（部門3件・勘定科目とも取得済み）
- **人事労務は `users/me` まで。`employees` は 400「事業所設定を完了させて下さい」**
- 工数管理は**使えない**（製品が入っていない）

### 次にやること

1. **freee人事労務（https://p.secure.freee.co.jp/）で初期設定3ステップを完了する**
   - ①給与規定 … 月末締め・翌月25日払い
   - ②働き方 … **固定時間制**／1日8時間・週40時間／打刻を使う
   - ③従業員を1名登録 … **架空の氏名で**。部門は会計側と揃えて「開発部門」を作る
   - ※右上の事業所名が2つとも「開発用テスト事業所」。**古い `12755401` でないか注意**
2. 完了したら、この順に叩いて判定
   - `GET /api/v1/employees`（company_id, year, month が必須）
   - `GET /api/v1/employees/{id}/time_clocks`
   - `GET /api/v1/employees/{id}/work_records/{date}`
3. 通ったら `REQUEST.md` を**新しい題材に書き直して** `/speckit-specify` へ

### ★REQUEST.md は書き直しが要る

いまの `REQUEST.md` は「工数→会計」で書いてある。**工数管理が使えないので前提が崩れた。**
新しい題材はこう:

> **勤怠はある。工数が無い。だから打刻に「何の仕事か」を足す。**
> freee人事労務の打刻・日次勤怠を読み、そこにプロジェクト（＝指図書番号）と工程を足して、
> 単価を掛けて労務費を出し、freee会計の部門・品目へ載せる。
> **技術部時代（勤怠×工数の一体入力）とまったく同じ判断を、40年後にもう一度する。**

- 単価は freee工数管理の `unit_costs` が使えないので、**こちらで持つ**（設計判断が1つ増えた）
- 会計側の受け皿は**部門が既に3件ある**（営業部・営業部門・開発部門）ので、そこに載せられる
